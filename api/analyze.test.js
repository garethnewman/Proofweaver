import{describe,it,expect}from'vitest';import{allowRequest,validateAnalysis}from'./analyze.js';
const valid=()=>({summary:'Summary',gap:'Gap',guardrail:'Guardrail',evidence:[{excerpt:'Output rose 12%.'}],claims:[{evidenceIndexes:[0,0,8]}],options:[{claimIndexes:[0,4]}]});
describe('analysis verification',()=>{it('accepts normalized verbatim excerpts and cleans graph indexes',()=>{const result=validateAnalysis(valid(),'Report:\nOutput   rose 12%.');expect(result.claims[0].evidenceIndexes).toEqual([0]);expect(result.options[0].claimIndexes).toEqual([0])});it('rejects invented excerpts',()=>{expect(()=>validateAnalysis(valid(),'Output fell.')).toThrow(/could not be verified/)});it('rejects claims without valid evidence',()=>{const result=valid();result.claims[0].evidenceIndexes=[7];expect(()=>validateAnalysis(result,'Output rose 12%.')).toThrow(/no verifiable evidence/)})});
describe('abuse protection',()=>{it('limits repeated requests within a window',()=>{const key=`test-${Date.now()}`;for(let i=0;i<8;i++)expect(allowRequest(key,1000+i)).toBe(true);expect(allowRequest(key,1010)).toBe(false);expect(allowRequest(key,700000)).toBe(true)})});
